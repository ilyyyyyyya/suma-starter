# Changelog

What changed in the vault. Newest first. One line per change.

## YYYY-MM-DD

- **Suma** set up the dashboard — live at `~/Desktop/code/12-suma/dashboard.html`. Source markdown in `vault/Suma/`. Rebuild with `python3 ~/Desktop/code/12-suma/build.py`.
- **Project One** seeded with first tasks.
- **Personal** initial admin items captured.
